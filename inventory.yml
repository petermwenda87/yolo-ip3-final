all:
  hosts:
    myserver:
      ansible_host: 127.0.0.1
      ansible_port: 2201
      ansible_user: vagrant
      ansible_private_key_file: .vagrant/machines/default/docker/private_key
      ansible_python_interpreter: /usr/bin/python3  # Ensuring it uses Python 3
